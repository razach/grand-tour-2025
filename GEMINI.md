# Gemini Workspace Context for YearEnd Trip

## Project Overview
This project contains an AI-driven advisor to help plan and execute a last-minute year-end trip for the user and their brother.

## Building and Running
1.  **Install Dependencies:**
    ```bash
    ./.venv/bin/pip install -r requirements.txt
    ```
2.  **Run the Advisor:**
    (Instructions to be added once implemented)

## Development Conventions
*   **Data:** Place raw data in `data/`.
*   **Source:** Code mainly in `src/`.
*   **Notebooks:** Use `notebooks/` for experimentation.
*   **Tests:** Add tests in `tests/`.
